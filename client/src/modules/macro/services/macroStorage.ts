import { MacroDefinition } from '@nexus/shared';

export async function loadMacros(): Promise<readonly MacroDefinition[]> {
  return [];
}

export async function saveMacros(_macros: readonly MacroDefinition[]): Promise<void> {}
